// Track 4 — Activity & Notifications
// Notification feed + full activity log (who did what, when).
export default function Page() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold">Activity & Notifications</h1>
      <p className="text-sm text-muted-foreground">
        Track 4 · Build this screen here. Notification feed + full activity log (who did what, when).
      </p>
    </div>
  );
}
