// Track 2 — Assets
// Register assets (auto tag AF-0001), search/filter, per-asset history.
export default function Page() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold">Assets</h1>
      <p className="text-sm text-muted-foreground">
        Track 2 · Build this screen here. Register assets (auto tag AF-0001), search/filter, per-asset history.
      </p>
    </div>
  );
}
