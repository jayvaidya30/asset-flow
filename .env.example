# Copy to .env and fill in. Each dev can point at a local or cloud Postgres.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/assetflow?schema=public"

# Secret for signing JWT session tokens. Generate with: openssl rand -base64 32
JWT_SECRET="change-me-to-a-long-random-string"
